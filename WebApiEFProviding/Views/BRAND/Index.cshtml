@model IEnumerable<WebApiEFModel.BRAND>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SUPPLIER.DSC_SUPPLIER)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DSC_BRAND)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SUPPLIER.DSC_SUPPLIER)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DSC_BRAND)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_BRAND }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_BRAND }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_BRAND })
        </td>
    </tr>
}

</table>
